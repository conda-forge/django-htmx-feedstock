schema_version: 1

context:
  name: django-htmx
  version: "1.24.1"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name | replace('-', '_') }}-${{ version }}.tar.gz
  sha256: 2750c05ff673e4c4a86f0d5e79aaff3c1bdd4b1c73089a4e7a3ad4aa4a008ee7

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
    - setuptools >=77
  run:
    - python >=${{ python_min }}
    - django >=4.2
    - asgiref >=3.6

tests:
  - python:
      imports:
        - django_htmx
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: Extensions for using Django with htmx.
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/adamchainz/django-htmx
  documentation: https://django-htmx.readthedocs.io/

extra:
  recipe-maintainers:
    - thewchan
